

const AIOrbCanvas = () => {
  return (
    <div style={{ width: '100%', height: '100%', background: 'radial-gradient(circle at center, var(--sunken), var(--canvas))', opacity: 0.5 }} />
  );
};

export default AIOrbCanvas;
